# 任务 02v2：主线信号放宽测试

你正在做 20260330_v2 补闭环调研，不是在写观点稿。

硬约束：
1. 主结果必须写入：`/Users/fengzhi/Downloads/git/testlixingren/docs/opportunity_strategies_20260330/result_v2_02_mainline_signal_relaxed.md`
2. 简版回执必须同时写入：`/Users/fengzhi/Downloads/git/testlixingren/docs/opportunity_strategies_20260330/dispatch_prompts_20260330_v2/results/02_v2_主线信号放宽测试_回执.md`
3. 必须使用 `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_nookbook` 做研究与样本外验证。
4. 必须使用 `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_nookbook` 做策略层回测（手动模拟，无时间限制）。
5. 必须单列 `2024-01-01` 之后样本外结果。
6. 不能写"预计/推断/估算"；没跑完就写"未完成"。

参考材料：
- `/Users/fengzhi/Downloads/git/testlixingren/docs/opportunity_strategies_20260330/result_01_mainline_signal_convergence.md`（原版：市值50-150亿，全年136信号）
- `/Users/fengzhi/Downloads/git/testlixingren/docs/opportunity_strategies_20260330/可信度总表_20260330.md`

任务目标：
测试放宽筛选条件后，主线信号数量的提升效果，验证收益是否保持稳定。

必须执行：
1. 测试以下筛选条件组合：
   - 原版：市值50-150亿 + 位置≤30% + 无连板
   - 放宽A：市值40-200亿 + 位置≤30% + 无连板
   - 放宽B：市值50-150亿 + 位置≤50% + 无连板
   - 放宽C：市值40-200亿 + 位置≤50% + 无连板
   - 放宽D：市值30-300亿 + 位置≤50% + 无连板
2. 统计每个版本：
   - 全年信号数量
   - 每日平均信号
   - 日内收益均值
   - 胜率
   - 最大收益均值
3. 对比各版本与原版的差异：
   - 信号数量提升倍数
   - 收益变化
   - 胜率变化
4. 筛选出最优放宽方案：
   - 信号数量足够（目标：每日≥1个）
   - 收益不显著下降（目标：>原版50%）
   - 胜率不低于原版80%
5. 输出推荐版本的完整规则

结果文档至少包含：
- 各版本实测结果对比表
- 信号数量vs收益曲线
- 最优放宽方案
- `2024-01-01` 后样本外验证
- 策略层回测（最优版本）
- 最终 `Go / Watch / No-Go`

回执文件必须写明：
- 推荐版本筛选条件
- 推荐版本信号数量
- 推荐版本收益与胜率
- 与原版对比数据
- 相关脚本路径
- 未完成项
- 是否可进入下一任务

通过门槛：
- 推荐版本信号数量≥原版2倍
- 推荐版本收益≥原版50%
- 推荐版本胜率≥原版80%
---

## 运行方式

```bash
cd /Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_nookbook
node run-strategy.js --strategy examples/first_board_signal_relaxed.py --timeout-ms 600000
cat output/joinquant-notebook-result-*.json | python3 -c "import json,sys; print(json.load(sys.stdin)['executions'][0]['textOutput'])"
```

## 脚本框架

```python
from jqdata import *
versions = {'原版':(50,150,0.30), '放宽A':(40,200,0.30), '放宽B':(50,150,0.50), '放宽C':(40,200,0.50), '放宽D':(30,300,0.50)}
# 测试各版本信号数量、收益、胜率
```
