# 任务 04：龙头底分型生死判断

你正在补闭环，不是在写观点稿。

硬约束：
1. 必须使用 `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_nookbook` 做严格滚动训练与样本外验证。
2. 必须使用 `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_strategy` 做策略层回测。
3. 必须单列 `2024-01-01` 之后样本外结果。
4. 必须披露：交易次数、胜率、盈亏比、年化收益、最大回撤、卡玛比率、连续亏损次数、月度收益分布、容量与滑点敏感性。
5. 禁止再写“预计/推断/估算/理论上/逻辑上应当”。没有实测结果就明确写“未完成”。
6. 只能修改本任务对应的 `result_*.md`，不要改其他结果文档。
7. 如果发现旧文档里的结论与新实测冲突，以新实测为准，直接改写，不要保留旧的错误结论。
8. 结果文档必须写成这 6 段：
   - 本轮实验设计
   - `joinquant_nookbook` 滚动训练结果
   - `2024-01-01` 后样本外结果
   - `joinquant_strategy` 策略层回测结果
   - 风险与失真来源
   - 最终 `Go / Watch / No-Go`

在以下文件上继续工作并直接改写为真实结果稿：
`/Users/fengzhi/Downloads/git/testlixingren/docs/opportunity_strategies_20260329/result_04_leader_bottom_fractal.md`

参考材料：
- `/Users/fengzhi/Downloads/git/testlixingren/聚宽有价值策略558/02 龙头底分型战法-两年23倍.txt`
- `/Users/fengzhi/Downloads/git/testlixingren/docs/opportunity_strategies_20260329/04_龙头底分型_核心机制.md`

任务目标：
这篇不要先优化，先判断值不值得继续投入。核心回答：
1. 严格分钟版是不是样本太少？
2. 日线简化版是否还有统计意义？
3. `2024-01-01` 后还有没有活性？

必须执行：
1. 在 `joinquant_nookbook` 先做信号普查：
   - 2021-2026 总信号数
   - `2024-01-01` 后信号数
   - 严格分钟版 vs 日线简化版
2. 如果严格分钟版信号极少，直接降级处理。
3. 简化版只保留三要素：
   - 主升浪背景
   - 底分型
   - 次日确认高开
4. 在 `joinquant_strategy` 做：
   - 持有1天
   - 持有3天
   - 持有5天
5. 明确判断它是否只是极少数案例驱动。

通过门槛：
- `2024-01-01` 后有足够样本
- 不是靠个别极端案例撑收益
- 简化版仍有合理盈亏比

最终结论只允许：
- `Go`
- `Watch`
- `No-Go`
并解释是否值得进入下一轮。
