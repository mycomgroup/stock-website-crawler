# 任务 06：宏观情绪择时去叙事化

你正在补闭环，不是在写观点稿。

硬约束：
1. 必须使用 `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_nookbook` 做严格滚动训练与样本外验证。
2. 必须使用 `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_strategy` 做策略层回测。
3. 必须单列 `2024-01-01` 之后样本外结果。
4. 必须披露：交易次数、胜率、盈亏比、年化收益、最大回撤、卡玛比率、连续亏损次数、月度收益分布、容量与滑点敏感性。
5. 禁止再写“预计/推断/估算/理论上/逻辑上应当”。没有实测结果就明确写“未完成”。
6. 只能修改本任务对应的 `result_*.md`，不要改其他结果文档。
7. 如果发现旧文档里的结论与新实测冲突，以新实测为准，直接改写，不要保留旧的错误结论。
8. 结果文档必须写成这 6 段：
   - 本轮实验设计
   - `joinquant_nookbook` 滚动训练结果
   - `2024-01-01` 后样本外结果
   - `joinquant_strategy` 策略层回测结果
   - 风险与失真来源
   - 最终 `Go / Watch / No-Go`

在以下文件上继续工作并直接改写为真实结果稿：
`/Users/fengzhi/Downloads/git/testlixingren/docs/opportunity_strategies_20260329/result_06_macro_sentiment_timing.md`

参考材料：
- `/Users/fengzhi/Downloads/git/testlixingren/聚宽有价值策略558/21 利用宏观经济数据的中长线策略深度研究.txt`
- `/Users/fengzhi/Downloads/git/testlixingren/docs/opportunity_strategies_20260329/06_宏观情绪_择时框架.md`

任务目标：
把这篇从“框架稿”改成“可验证的仓位调节器实测稿”，回答：
1. 宏观层是否真的有用？
2. 宏观层是否只是叙事？
3. 宏观+情绪是否真的优于仅情绪？

必须执行：
1. 在 `joinquant_nookbook` 只保留有发布时间约束、可避免未来函数的变量：
   - PMI绝对值
   - PMI斜率
   - 新订单/生产扩散
   - 库存信号
2. 必须处理发布时间滞后，不能偷看未来。
3. 先做指数层有效性验证，再接策略层。
4. 在 `joinquant_strategy` 跑三组：
   - 无择时
   - 仅情绪择时
   - 宏观+情绪择时
5. 先接两个子策略：
   - `01 首板低开`
   - `02 弱转强`
6. 单列 `2024-01-01` 后样本外结果。

通过门槛：
- `宏观+情绪` 的卡玛比率高于 `无择时`
- 最大回撤明显下降
- 年化下降不超过 `25%`
- 空仓/低仓位区间有明确解释力

如果宏观层没用，要直接下结论“只保留情绪层，砍掉宏观层”。
