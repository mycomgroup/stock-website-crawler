# 任务 05：情绪总开关实证化

你正在补闭环，不是在写观点稿。

硬约束：
1. 必须使用 `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_nookbook` 做严格滚动训练与样本外验证。
2. 必须使用 `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_strategy` 做策略层回测。
3. 必须单列 `2024-01-01` 之后样本外结果。
4. 必须披露：交易次数、胜率、盈亏比、年化收益、最大回撤、卡玛比率、连续亏损次数、月度收益分布、容量与滑点敏感性。
5. 禁止再写“预计/推断/估算/理论上/逻辑上应当”。没有实测结果就明确写“未完成”。
6. 只能修改本任务对应的 `result_*.md`，不要改其他结果文档。
7. 如果发现旧文档里的结论与新实测冲突，以新实测为准，直接改写，不要保留旧的错误结论。
8. 结果文档必须写成这 6 段：
   - 本轮实验设计
   - `joinquant_nookbook` 滚动训练结果
   - `2024-01-01` 后样本外结果
   - `joinquant_strategy` 策略层回测结果
   - 风险与失真来源
   - 最终 `Go / Watch / No-Go`

在以下文件上继续工作并直接改写为真实结果稿：
`/Users/fengzhi/Downloads/git/testlixingren/docs/opportunity_strategies_20260329/result_05_consecutive_board_leader_sentiment.md`

参考材料：
- `/Users/fengzhi/Downloads/git/testlixingren/聚宽有价值策略558/07 连板龙头策略.txt`
- `/Users/fengzhi/Downloads/git/testlixingren/docs/opportunity_strategies_20260329/05_连板龙头_情绪周期.md`
- 当前已生成脚本：
  - `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_nookbook/examples/sentiment_cycle_research.py`
  - `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_nookbook/examples/sentiment_switch_backtest.py`

任务目标：
不要再写逻辑分析，直接回答：
1. 情绪开关到底有没有真实增益？
2. 最有效的情绪指标组合是什么？
3. 应该做硬开关还是仓位调节器？

必须执行：
1. 在 `joinquant_nookbook` 比较 4 组情绪指标：
   - `最高连板`
   - `最高连板 + 涨停家数`
   - `最高连板 + 涨停家数 + 涨跌停比`
   - `最高连板 + 涨停家数 + 涨跌停比 + 晋级率`
2. 至少接入两个子策略实测：
   - `01 首板低开`
   - `02 弱转强`
3. 在 `joinquant_strategy` 做三组对照：
   - 无开关
   - 硬开关
   - 仓位调节器
4. 单列 `2024-01-01` 后样本外表现。
5. 输出必须是清晰对照表，不要只写文字判断。

通过门槛：
- 至少两个子策略的最大回撤改善 `>20%`
- 卡玛比率提升
- 年化下降不超过 `20%`

若只对某一策略有效，要明确写“只适用于哪类子策略”。
