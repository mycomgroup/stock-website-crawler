# 任务 07：组合仅组合通过项

你正在补闭环，不是在写观点稿。

硬约束：
1. 必须使用 `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_nookbook` 做严格滚动训练与样本外验证。
2. 必须使用 `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_strategy` 做策略层回测。
3. 必须单列 `2024-01-01` 之后样本外结果。
4. 必须披露：交易次数、胜率、盈亏比、年化收益、最大回撤、卡玛比率、连续亏损次数、月度收益分布、容量与滑点敏感性。
5. 禁止再写“预计/推断/估算/理论上/逻辑上应当”。没有实测结果就明确写“未完成”。
6. 只能修改本任务对应的 `result_*.md`，不要改其他结果文档。
7. 如果发现旧文档里的结论与新实测冲突，以新实测为准，直接改写，不要保留旧的错误结论。
8. 结果文档必须写成这 6 段：
   - 本轮实验设计
   - `joinquant_nookbook` 滚动训练结果
   - `2024-01-01` 后样本外结果
   - `joinquant_strategy` 策略层回测结果
   - 风险与失真来源
   - 最终 `Go / Watch / No-Go`

在以下文件上继续工作并直接改写为真实结果稿：
`/Users/fengzhi/Downloads/git/testlixingren/docs/opportunity_strategies_20260329/result_07_portfolio_combination_sizing.md`

前提：
只能组合已经通过验证的子策略，不允许继续用“逻辑估算相关性”。

任务目标：
把这篇从“想象中的组合设计”改成“真实通过策略组合结果”，回答：
1. 组合后是否真的比最强单策略更好？
2. 哪种权重方式最优？
3. 动态权重值不值得复杂化？

必须执行：
1. 仅使用已经通过验证的子策略净值序列。
2. 相关性必须用真实净值序列计算，不准估算。
3. 在 `joinquant_strategy` 做三组：
   - 等权
   - 风险平价
   - 情绪驱动动态权重
4. 与“最强单策略”做正面对照。
5. 单列 `2024-01-01` 后样本外组合结果。

通过门槛：
- 组合卡玛比率高于最强单策略
- 回撤真实下降
- 年化不要被稀释太多

如果组合不如单策略，必须明确写“暂不做组合，先做单策略”。
