# 任务 02：弱转强闭环重做

你正在补闭环，不是在写观点稿。

硬约束：
1. 必须使用 `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_nookbook` 做严格滚动训练与样本外验证。
2. 必须使用 `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_strategy` 做策略层回测。
3. 必须单列 `2024-01-01` 之后样本外结果。
4. 必须披露：交易次数、胜率、盈亏比、年化收益、最大回撤、卡玛比率、连续亏损次数、月度收益分布、容量与滑点敏感性。
5. 禁止再写“预计/推断/估算/理论上/逻辑上应当”。没有实测结果就明确写“未完成”。
6. 只能修改本任务对应的 `result_*.md`，不要改其他结果文档。
7. 如果发现旧文档里的结论与新实测冲突，以新实测为准，直接改写，不要保留旧的错误结论。
8. 结果文档必须写成这 6 段：
   - 本轮实验设计
   - `joinquant_nookbook` 滚动训练结果
   - `2024-01-01` 后样本外结果
   - `joinquant_strategy` 策略层回测结果
   - 风险与失真来源
   - 最终 `Go / Watch / No-Go`

在以下文件上继续工作并直接改写为真实结果稿：
`/Users/fengzhi/Downloads/git/testlixingren/docs/opportunity_strategies_20260329/result_02_weak_to_strong_auction.md`

参考材料：
- `/Users/fengzhi/Downloads/git/testlixingren/聚宽有价值策略558/21 【原创】年化123%最大回撤14%涨停弱转强竞价战法.txt`
- `/Users/fengzhi/Downloads/git/testlixingren/docs/opportunity_strategies_20260329/02_弱转强竞价_核心机制.md`

任务目标：
把当前这篇从“预期稿”改成“实测稿”，核心回答：
1. 竞价高开幅度、竞价量比、左压突破，谁贡献最大？
2. `2024-01-01` 之后这个方向还能不能赚钱？
3. 开盘近似竞价是否导致收益高估？
4. 最终应保留的可执行版本是什么？

必须执行：
1. 在 `joinquant_nookbook` 做参数网格：
   - 高开幅度：`0~2 / 2~4 / 4~6 / 6~8`
   - 量比：`>3 / >5 / >8`
   - 市值：`20~50亿 / 30~70亿 / >70亿`
   - 成交额：`>5亿 / >7亿 / >10亿`
   - 左压阈值：`0.8 / 0.9 / 1.0`
2. 做两套版本：
   - 严格贴近竞价版
   - 开盘后可执行近似版
3. 在 `joinquant_strategy` 做 4 组对照：
   - 无情绪过滤
   - 接入情绪过滤
   - 尾盘统一卖
   - 时间止损版
4. 单列 `2024-01-01` 后样本外。
5. 明确写出“最优版本”和“应砍掉的规则”。

通过门槛：
- `2024-01-01` 后样本外年化 `>25%`
- 最大回撤 `<25%`
- 卡玛比率 `>1.2`
- 交易数不能过少

如果跑不出门槛，也要如实写成 `Watch` 或 `No-Go`，不要硬写 `Go`。
