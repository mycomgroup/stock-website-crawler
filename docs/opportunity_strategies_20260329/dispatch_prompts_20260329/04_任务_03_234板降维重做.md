# 任务 03：234板降维重做

你正在补闭环，不是在写观点稿。

硬约束：
1. 必须使用 `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_nookbook` 做严格滚动训练与样本外验证。
2. 必须使用 `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_strategy` 做策略层回测。
3. 必须单列 `2024-01-01` 之后样本外结果。
4. 必须披露：交易次数、胜率、盈亏比、年化收益、最大回撤、卡玛比率、连续亏损次数、月度收益分布、容量与滑点敏感性。
5. 禁止再写“预计/推断/估算/理论上/逻辑上应当”。没有实测结果就明确写“未完成”。
6. 只能修改本任务对应的 `result_*.md`，不要改其他结果文档。
7. 如果发现旧文档里的结论与新实测冲突，以新实测为准，直接改写，不要保留旧的错误结论。
8. 结果文档必须写成这 6 段：
   - 本轮实验设计
   - `joinquant_nookbook` 滚动训练结果
   - `2024-01-01` 后样本外结果
   - `joinquant_strategy` 策略层回测结果
   - 风险与失真来源
   - 最终 `Go / Watch / No-Go`

在以下文件上继续工作并直接改写为真实结果稿：
`/Users/fengzhi/Downloads/git/testlixingren/docs/opportunity_strategies_20260329/result_03_234_board.md`

参考材料：
- `/Users/fengzhi/Downloads/git/testlixingren/聚宽有价值策略558/93 234板介入-2023年14倍-无未来.txt`
- `/Users/fengzhi/Downloads/git/testlixingren/docs/opportunity_strategies_20260329/03_234板_核心机制.md`

任务目标：
不要再把 `2/3/4板` 混在一起讲。必须拆开并回答：
1. 二板、三板、四板，哪个在 `2024-01-01` 后还活着？
2. 考虑买不到和滑点后，哪个板位还能赚钱？
3. 情绪开关接入后是否显著改善？

必须执行：
1. 分三个子策略分别回测：
   - 二板
   - 三板
   - 四板
2. 每个板位分别做：
   - 无情绪过滤
   - 接入情绪开关
   - 接入宏观+情绪
3. 必须加入成交可实现性场景：
   - 只做非涨停开盘
   - 涨停排板成交率 `30%`
   - 涨停排板成交率 `10%`
4. 披露滑点与买不到导致的收益折损。
5. 如果四板明显失效，可以明确砍掉，只保留二板/三板。

通过门槛：
- 至少一个板位在 `2024-01-01` 后样本外仍为正
- 考虑成交率与滑点后仍为正
- 最大回撤不过分失控

最后必须给出一句明确结论：
- “只保留二板”
- “保留二板+三板”
- “整体 No-Go”
三选一，不要模糊。
