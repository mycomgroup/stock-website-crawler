# 任务 08：风控 A/B 测试

你正在补闭环，不是在写观点稿。

硬约束：
1. 必须使用 `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_nookbook` 做严格滚动训练与样本外验证。
2. 必须使用 `/Users/fengzhi/Downloads/git/testlixingren/skills/joinquant_strategy` 做策略层回测。
3. 必须单列 `2024-01-01` 之后样本外结果。
4. 必须披露：交易次数、胜率、盈亏比、年化收益、最大回撤、卡玛比率、连续亏损次数、月度收益分布、容量与滑点敏感性。
5. 禁止再写“预计/推断/估算/理论上/逻辑上应当”。没有实测结果就明确写“未完成”。
6. 只能修改本任务对应的 `result_*.md`，不要改其他结果文档。
7. 如果发现旧文档里的结论与新实测冲突，以新实测为准，直接改写，不要保留旧的错误结论。
8. 结果文档必须写成这 6 段：
   - 本轮实验设计
   - `joinquant_nookbook` 滚动训练结果
   - `2024-01-01` 后样本外结果
   - `joinquant_strategy` 策略层回测结果
   - 风险与失真来源
   - 最终 `Go / Watch / No-Go`

在以下文件上继续工作并直接改写为真实结果稿：
`/Users/fengzhi/Downloads/git/testlixingren/docs/opportunity_strategies_20260329/result_08_risk_control_takeprofit.md`

参考材料：
- `/Users/fengzhi/Downloads/git/testlixingren/docs/opportunity_strategies_20260329/08_止损止盈_风控体系.md`
- 已通过或较强的子策略结果，优先使用 `01` 与 `02`

任务目标：
不是继续设计风控规则，而是实测回答：
1. 单票时间止损是否最值当？
2. 组合熔断是否过严？
3. 哪一层风控最适合机会仓？

必须执行：
1. 只挂接在真实子策略上测试，优先：
   - `01 首板低开`
   - `02 弱转强`
2. 在 `joinquant_strategy` 跑三组：
   - A：基线版
   - B：单票时间止损版
   - C：单票 + 组合熔断版
3. 输出必须包含：
   - 年化
   - 最大回撤
   - 卡玛
   - 交易次数
   - 连续亏损次数
   - 休息天数
   - 收益回吐幅度
4. 单列 `2024-01-01` 后样本外。
5. 最终只允许给一个主推荐方案，不能“两者都行”。

通过门槛：
- 若 B 比 A 的卡玛显著提升且年化损失不大，则 B 为主方案
- 若 C 的收益损失过大，则只能作为保守备选
