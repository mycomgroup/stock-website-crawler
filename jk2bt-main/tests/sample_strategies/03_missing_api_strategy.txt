def initialize(context):
    g.stock = '600519.XSHG'
    log.info('初始化缺失API测试策略')
    run_daily(rebalance, time='open')

def rebalance(context):
    ticks = get_ticks(g.stock)
    order_target_value(g.stock, context.portfolio.total_value * 0.95)