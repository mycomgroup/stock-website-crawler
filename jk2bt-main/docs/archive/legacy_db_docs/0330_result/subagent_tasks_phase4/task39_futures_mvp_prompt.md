# Task 39: 股指期货 MVP

## 任务目标

为 `CCFX` 股指期货建立最小可用回测能力，至少让简单择时或主力合约策略进入“可验证”阶段。

## 建议写入目录

- `jqdata_akshare_backtrader_utility/market_data/`
- `jqdata_akshare_backtrader_utility/`
- `tests/`
- `docs/0330_result/`

## 负责范围

- 合约识别
- 行情数据
- 合约乘数
- 保证金 / 持仓最小模型

## 给子 agent 的提示词

你负责股指期货 MVP。

目标不是一步到位复刻真实期货系统，而是先让简单期货策略能进入可回放、可验证阶段。

最小范围：
- `IF / IC / IH` 合约识别
- 合约元数据
- 日线行情和必要分钟行情
- 合约乘数
- 保证金占用的最小模型
- 主力合约选择或显式固定合约

要求：
- 不要一开始就做复杂移仓引擎
- 先让简单单合约策略能测
- 风险与未实现部分写清楚

## 任务验证

- 增加 1 到 2 个简单期货样例
- 至少验证一个 `CCFX` 合约能拿到数据并进入回测
- 输出 `docs/0330_result/task39_futures_mvp_result.md`

## 任务成功总结

- 期货能力从 0 变成最小可用
- 为真正的多资产策略打开入口

