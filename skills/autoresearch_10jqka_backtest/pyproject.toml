[project]
name = "autoresearch-10jqka-backtest"
version = "0.1.0"
description = "问财公式回测自动研究系统"
requires-python = ">=3.10"

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"