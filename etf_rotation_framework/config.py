# -*- coding: utf-8 -*-
"""
ETF轮动策略统一配置文件
"""

# ============================================================
# 1. 候选池构建参数
# ============================================================

POOL_CONFIG = {
    # ETF筛选条件
    "start_date_threshold": "2021-01-01",  # 成立时间早于此日期
    "end_date_check": True,  # 是否检查ETF是否仍在存续
    "min_avg_volume": 1e7,  # 最低日均成交额（元）
    "volume_window": 300,  # 计算平均成交额的窗口（天）
    # 聚类参数
    "price_window": 450,  # 用于聚类的价格走势窗口（天）
    "n_clusters": 24,  # KMeans聚类簇数
    "random_state": 42,  # 随机种子
    # 相关性过滤
    "corr_threshold": 0.85,  # 相关系数阈值
}

# ============================================================
# 2. 因子计算参数
# ============================================================

FACTOR_CONFIG = {
    # 动量因子
    "momentum_windows": [3, 5, 10, 20, 30, 60],  # 动量计算窗口
    "momentum_method": "slope",  # 动量计算方法: 'slope' 或 'return'
    # 因子IC分析
    "ic_forward_periods": [1, 5, 10, 20, 30, 60, 90],  # IC计算的未来收益周期
}

# ============================================================
# 3. 择时过滤参数
# ============================================================

TIMING_CONFIG = {
    # 市场宽度
    "breadth_window": 20,  # MA窗口
    "breadth_threshold": 0.5,  # 宽度阈值（50%股票在MA之上）
    # RSRS择时
    "rsrs_window": 60,  # RSRS计算窗口
    "rsrs_method": "damped",  # RSRS方法: 'original' 或 'damped'
    # 综合择时
    "timing_mode": "combined",  # 择时模式: 'breadth', 'rsrs', 'combined', 'none'
}

# ============================================================
# 4. 策略执行参数
# ============================================================

STRATEGY_CONFIG = {
    # 持仓设置
    "hold_count": 5,  # 持仓ETF数量
    "hold_period": 10,  # 持仓周期（天）
    # 调仓设置
    "rebalance_threshold": 0.1,  # 排名变化超过此比例触发调仓
    # 交易成本
    "commission_rate": 0.0003,  # 单边佣金率
    "slippage_rate": 0.001,  # 单边滑点率
    # 风控设置
    "max_drawdown_limit": 0.15,  # 最大回撤限制
    "position_limit": 1.0,  # 最大仓位
}

# ============================================================
# 5. 回测参数
# ============================================================

BACKTEST_CONFIG = {
    "start_date": "2020-01-01",
    "end_date": "2024-12-31",
    "benchmark": "000300.XSHG",  # 基准指数
    "initial_capital": 1000000,  # 初始资金
}

# ============================================================
# 6. 数据源配置
# ============================================================

DATA_CONFIG = {
    "data_source": "jqdata",  # 数据源: 'jqdata', 'akshare', 'tushare'
    "cache_enabled": True,  # 是否启用缓存
    "cache_dir": "./cache",  # 缓存目录
}
