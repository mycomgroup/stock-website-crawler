# RSS API 数据与 Web 页面比对说明文档 (模板)

## 1. 基本信息
- **任务类别**: [RSS / API / MCP / hao123] (当前: RSS)
- **数据源名称**: [填写数据源名称，如：新浪财经 RSS]
- **RSS API 地址**: `[填写获取 RSS 数据的 API Endpoint]`
- **目标 Web 页面地址**: `[填写对应的实际网页 URL]`
- **测试时间**: YYYY-MM-DD
- **测试人员**: [填写测试人员姓名]

## 2. 比对目标
通过 API 方式获取 RSS 数据，并与实际 Web 页面展示的数据进行全面比对，评估 RSS API 数据的**完整性**、**准确性**和**时效性**，以决定后续数据获取策略（例如：是否可以直接使用 RSS API 替代网页爬虫，或者需要两者结合）。

## 3. 字段映射与数据完整性比对

| 字段名称 | Web 页面展示情况 | RSS API 返回情况 | 比对结果 (一致/缺失/差异) | 备注/差异说明 |
| :--- | :--- | :--- | :--- | :--- |
| **标题 (Title)** | [如：完整展示] | [如：包含] | [一致] | |
| **正文/摘要 (Content/Description)** | [如：全文展示，含图文] | [如：仅纯文本摘要，无图片] | [差异] | RSS 通常只提供摘要，若需全文需额外爬取网页 |
| **发布时间 (PubDate)** | [如：精确到秒] | [如：精确到分] | [基本一致] | 注意时区是否需要格式化处理 |
| **作者/来源 (Author/Source)** | [如：展示来源网站] | [如：无此字段] | [缺失] | |
| **原文链接 (Link)** | [如：相对路径或带有 tracking 参数] | [如：干净的绝对路径] | [差异] | API 返回的链接通常更干净，可直接使用 |
| **分类/标签 (Category/Tags)** | [如：有分类标签] | [如：无] | [缺失] | |
| **缩略图 (Enclosure/Image)** | [如：文章首图] | [如：XML/JSON 中有图] | [一致] | |

## 4. 数据格式与质量分析
- **HTML 标签处理**: RSS API 返回的 `description` 或 `content` 字段中是否包含 HTML 标签？是否需要清洗（如去除 `<script>`, `<iframe>` 等）？
  - *[分析结果：例如，包含基础排版标签，可以直接渲染，无须深度清洗。]*
- **特殊字符与编码**: 是否存在乱码、CDATA 未正确解析或转义字符问题？
  - *[分析结果：例如，编码正常，UTF-8。]*

## 5. 时效性与接口限制分析
- **更新延迟**: Web 页面发布新内容后，RSS API 大约多久能同步更新？
  - *[分析结果：例如，几乎实时同步 / 存在约 5-10 分钟延迟。]*
- **数据列表长度**: RSS API 一次请求返回多少条数据？是否支持分页或历史回溯？
  - *[分析结果：例如，固定返回最新 20 条，不支持分页，无法获取历史数据。]*

## 6. 结论与后续接入方案
### 6.1 结论概览
*[在此总结 RSS API 的可用性。例如：RSS API 数据质量较高，标题和链接完全准确，但正文仅为摘要。时效性极佳，适合做实时监控。]*

### 6.2 后续接入策略建议 (Next Steps)
- [ ] **纯 API 接入**: [是/否] 数据完整且满足业务需求，直接基于 API 编写接入脚本。
- [ ] **混合抓取接入 (API + 爬虫)**: [是/否] 如果需要全文，使用 RSS API 快速获取最新列表和链接，再通过爬虫进入详细页抓取正文。
- [ ] **数据清洗任务**: [列出需要编写的清洗逻辑，如去除 HTML 标签、格式化时间等]

---
**附录：API 请求示例与响应结构**
```json
// 在此处粘贴 RSS API 返回的典型 JSON 数据结构片段，方便后续开发与字段映射参考
{
  "title": "...",
  "link": "...",
  "description": "...",
  "pubDate": "..."
}
```