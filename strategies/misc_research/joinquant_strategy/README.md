# JoinQuant Strategy Sources

This directory is the canonical home for the local JoinQuant strategy Python files.

Legacy paths under `skills/joinquant_strategy/*.py` are kept as symlinks so older commands,
docs, and local scripts can continue to reference the previous locations without changes.
