# Web API 数据与页面比对说明文档 (模板)

## 1. 基本信息
- **任务类别**: API 接口数据验证
- **目标平台/站点**: [填写平台名称，如：雪球、同花顺等]
- **API 接口地址**: `[GET/POST] https://api.example.com/...`
- **对应 Web 页面**: `[填写展示该 API 数据的对应前端页面 URL]`
- **测试时间**: YYYY-MM-DD
- **测试人员**: [填写测试人员姓名]

## 2. 比对目标
通过直接调用后端或第三方 Web API，抓取核心数据结构，并与前端页面的展示效果进行逐一比对。评估 API 数据的**全面性**、**可用性**以及**隐藏字段价值**，确认该 API 是否可替代页面爬虫进行数据采集。

## 3. 核心字段映射与比对

| Web 页面展示字段 | API 响应字段 (JSON Key) | 数据类型 | 比对结果 (一致/需转换/缺失) | 备注/转换规则说明 |
| :--- | :--- | :--- | :--- | :--- |
| **例如：股票名称** | `name` / `symbolName` | String | [一致] | 直接可用 |
| **例如：最新价格** | `currentPrice` | Number | [需转换] | API 可能是整数(分)，前端展示需除以 100 |
| **例如：发布时间** | `timestamp` | Long | [需转换] | API 返回 Unix 时间戳，需格式化为 YYYY-MM-DD |
| **例如：详情链接** | [无直接字段] | N/A | [缺失] | 需要通过 ID 拼接: `https://.../detail/{id}` |

## 4. API 隐藏高价值字段分析
*（记录页面上没有展示，但 API 返回的对分析有用的额外数据）*
- **隐藏字段 1**: `[字段名]` - `[字段业务含义，如：内部阅读量、真实点赞数、系统标签等]`
- **隐藏字段 2**: `[字段名]` - `[字段业务含义]`

## 5. 接口调用限制与反爬机制
- **鉴权要求 (Auth)**: [例如：需 Header 携带 Token、Cookie，或存在签名验证 (Sign)]
- **请求频次限制 (Rate Limit)**: [例如：单 IP 每秒限 5 次，超出返回 429]
- **参数动态性**: [例如：是否存在动态生成的时间戳、随机盐值或加密参数]

## 6. 结论与后续接入方案
### 6.1 结论概览
*[总结 API 方案的优劣，例如：API 数据格式极其规整，且包含额外的深度数据，但存在严格的请求频率限制。]*

### 6.2 接入策略建议 (Next Steps)
- [ ] **纯 API 直接接入**: 数据完整，鉴权简单，优先采用。
- [ ] **模拟浏览器抓取 API (Puppeteer/Playwright 拦截)**: 接口签名机制复杂，难以直接用 requests 模拟，需借助无头浏览器拦截 Network 响应。
- [ ] **需进一步逆向分析**: 包含动态加密参数（如 `sign`, `token`），需解密。

---
**附录：API 请求参数与响应结构示例**
### 请求头 (Headers) / 参数 (Payload)
```json
{
  "page": 1,
  "pageSize": 20,
  "token": "..."
}
```

### 响应体 (Response JSON)
```json
{
  "code": 200,
  "data": {
    "items": [
      {
        "id": 12345,
        "title": "...",
        "price": 100.5
      }
    ]
  }
}
```